# リール制作パイプライン（2026-07-27 フェイシャルリールで確立・恒久版）

このフォルダは「次のリールを確認2回だけで完成させる」ための実働スクリプト一式。
2026-07のフェイシャルリール制作（v11まで11回作り直し）で確定した形。**次回はこれを起点にする（ゼロから書き直さない）**。

彩さんの確認ポイントは**2回だけ**：①ナレーション原稿＋キャプション ②完成3点セット。
それ以外の判断（クリップ選定・明るさ・演出）は下の工程とルールで機械的に決める。
新しい創作要素（AI画像の方向性など）が必要な場合は、①のタイミングでまとめて1回で見せる。

## ファイル
- `telv.py` … テロップ・ヘッダー描画（明朝W6白／ゴシックW8黄／金グラデ・句読点ルール・自動縮小）
- `build_body.py` … 本編組み立て（カット定義・色補正gamma・微ズーム交互・暗転・ヘッダー・声同期テロップ・白フラッシュ・BA）
- `build_final.py` … CTA連結＋音声ミックス（ナレーション・BGM dynaudnorm+ダッキング・SE・末尾フェード）
- `selfcheck.py` … 納品前セルフチェック（下記⑦）

## 工程（この順番厳守）
1. **素材の全数コンタクトシート化**（フェイシャル素材/ボディ素材/共通素材を全部見る。共通素材の店内・日常・清掃カットを忘れない）→ 各カットの明るさ(mean)をnumpyで実測
2. **原稿・キャプション → 彩さん確認**（確認ポイント①。AI画像を使うならこの時に候補シートも見せる）
3. **音声**：彩の声TTS（eleven_v3・API化済み `ELEVENLABS_API_KEY`）→ 文末の。境界だけで分割（読点の谷は使わない・字数÷声長6〜9字/秒で検算）→ 0.7秒間で再連結 → onset確定。イントネーションNGが出たら `narration-intonation-fix-sts` メモリの手順（録音をお手本にTTS複数テイク→F0相関で選ぶ。録音の直接挿入はNG＝声質が変わる）
4. **カット割り当て**：ナレーション各文に素材を当てる。感情が動く画を優先（1c）・モデルは複数人混ぜる・BA≥6秒/1回/後半・冒頭は0秒からフック（前置き禁止）・サロン雰囲気はブランド文に自然に挟む
5. **build_body.py を今回の onsets/カット/gamma に書き換えて実行**（gamma=log(mean/255)/log(142/255)で肌平均140に統一・白飛びはcurves前置き）
6. **build_final.py 実行**（タイミング定数：ダッキング=金文言区間・フラッシュ=BA直前・CTA開始）
7. **selfcheck.py 実行 → ALL PASS になるまで納品しない**（尺・全カット明るさ108-178・声同期・テロップ描画・セーフゾーンy285-1635）
8. **表紙**：CanvaテンプレDAG-ADhUOmA（フェイシャル）/DAHO39cnB7w（痩身）をcopy→read-design(open_transaction)→replace_text（見出しだけ）→commit→export png原寸
9. 3点セットを Drive「リール/投稿準備」に配置 → 彩さん確認（確認ポイント②）→「投稿して」で投稿キューへ

## 今回の三大失敗と対策（再発防止）
- **AIっぽさ**：AI素材は「スマホで普通に撮れる画」だけ（シネマ光・金色逆光・過剰ボケ禁止／顔はぼかしでなくアングルで消す／鏡はフレーム外）。プロンプトに candid smartphone photo / flat daylight。生成後は顔・指・反射を原寸で目視
- **ルール無視**：作る前に memory の bemolle-reel-template.md を全文読み、工程4〜7で機械的に適用。「1個直すたびに場当たり修正」をしない＝直しはこのREADMEの工程に戻って該当段からやり直す
- **明るさ**：目視でなく必ず実測（選ぶ瞬間のフレームのmean）→gamma補正→selfcheckで最終実測。BA・彩さん承認済みの明るいカウンセリング素材(0011-0047)は触らない

## 音声・素材の既知の値
- 彩の声 voice_id=`YKi6CoHElTZtKlJVEkPN`／TTS=eleven_v3 stability0.5／間=文間0.7秒
- BGM=`リール/BGM_ElegantSanctuary.mp3`（dynaudnorm→0.18・金区間0.10・CTA0.24・末尾1.5sフェード）
- CTA連結=プラペン: 体験価格→リール最後NEW／痩身: ダイエットプログラム→リール最後NEW
